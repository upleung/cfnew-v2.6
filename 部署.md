# CF-New v2.7-fixed / Cloudflare Pages 部署

## 1. 推荐部署方式

本版本使用 Cloudflare Pages Advanced Mode，最终上传的核心文件是 `_worker.js`。

可以直接把项目中的 `_worker.js` 放入 Pages Functions/Advanced Worker 产物，或者上传 GitHub Actions 生成的 `Pages.zip`。

Cloudflare 控制台中的 Pages 项目建议把 Runtime compatibility date 设置为 `2026-01-20`，与上游 cfnew 当前文档的兼容日期保持一致。

## 2. 必须变量

`u`：UUID。建议使用你自己的 UUID；本仓库默认值只是为了兼容原 v2.6 源码。

`d`：自定义路径，可选，例如 `cfnew-xxxxx`。不填时使用 `/UUID`。

## 3. KV 绑定

推荐建立以下 4 个 KV namespace，并将绑定名严格设置为：

- `CFNEW_CONFIG`
- `CFNEW_IP_POOL`
- `CFNEW_FALLBACK`
- `CFNEW_NETWORK_TYPE`

其中只有 `CFNEW_CONFIG` 是面板“保存地区 / 三网策略”所必需；其余 3 个没有绑定时 Worker 会使用内置默认数据。

### CFNEW_CONFIG

KV key：`config`

value 示例：

```json
{
  "wk": "SG",
  "network": "auto",
  "epd": "yes",
  "epi": "yes"
}
```

### CFNEW_IP_POOL

KV key：`list`

可以放 JSON 数组，或每行 `IP[:PORT]#节点名称`。

### CFNEW_FALLBACK

KV key：`list`

可以放 JSON 数组，例如：

```json
[
  {"domain":"ProxyIP.CMLiussss.net","regionCode":"","port":443},
  {"domain":"ProxyIP.SG.CMLiussss.net","regionCode":"SG","port":443}
]
```

### CFNEW_NETWORK_TYPE

预留给你后续维护 ASN / 网络类型映射。当前 Worker 已有常用 ASN 的内置基础识别，未知网络会回退到 `all`。

## 4. 节点路径

本版本修复了 v2.6 的 path mapping：

- 无 `d`：WebSocket 节点路径为 `/UUID?ed=2048...`，订阅为 `/UUID/sub`
- 有 `d=cfnew-xxxxx`：WebSocket 节点路径为 `/cfnew-xxxxx?ed=2048...`，订阅为 `/cfnew-xxxxx/sub`

Worker 不再对任意 WebSocket 路径直接建立代理连接。

## 5. 节点生成

默认会生成：

- 4 个优选域名 × 443/80 = 8 个
- 9 个三网优选 IP × 443/80 = 18 个
- Worker 原生入口 × 443/80 = 2 个

默认共 28 个节点；绑定 `CFNEW_IP_POOL` 后数量跟随 KV 池变化。

## 6. v2.7-fixed 核心修复

### 首包解析

支持 WebSocket 首包分片，不再假设 VLESS 头部必须完整落在一个 message 中。

### Remote socket

连接成功与“收到首字节”分离；仅在约 3.5 秒无首字节时触发 fallback，修复原版 `hasData` 同步判断导致的立即 fallback 竞速。

### fallback

默认包含需求文档要求的：

```js
const CF_FALLBACK_IPS = ['ProxyIP.CMLiussss.net'];
```

之后再按地区和 KV fallback 池继续尝试。

### Cloudflare 自回源

Worker 对 Cloudflare 自身 IP 范围的 TCP 出站可能触发阻断或 `TCP Loop detected`。因此 Cloudflare 保护站点直连失败时，Worker 才会进入 ProxyIP fallback，而不是重复对 Cloudflare 目标做回源。

### UI

保留原来的 Glass UI 视觉风格、背景、卡片、按钮体系；只修复地区下拉菜单为真正的透明毛玻璃菜单，并把“保存地区”从纯 alert 改为真实 KV API。

## 7. 测试顺序

1. 打开 `/UUID` 或 `/<d>`，确认 UI 可加载。
2. 打开 `/UUID/api/status` 或 `/<d>/api/status`，确认 `ok=true`、`nodeCount` 正常。
3. 打开 `/UUID/sub`，确认返回 Base64 订阅内容。
4. 客户端导入 443 TLS 节点，先测试 Google / YouTube。
5. 再测试 80 WS 节点。
6. 最后测试 Cloudflare 官网及 Cloudflare 保护站点。

若 443 正常而 80 不正常，不应直接判定 Worker 协议代码错误：80 线路还取决于你所选优选 IP/域名是否在 Cloudflare edge 上提供可用的 HTTP/WS 路由。

## 8. 一个重要限制

本 v2.7-fixed 是以“VLESS + WebSocket + TCP socket”可用性为第一优先级的修复版本。它没有伪装宣称完整实现 Trojan/xHTTP；如果后续需要把 Trojan/xHTTP 也加入 UI 和订阅生成，应继续移植上游当前版本对应的协议实现，而不要用简化 parser 冒充完整支持。
